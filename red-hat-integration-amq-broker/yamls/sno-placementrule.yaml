---
apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: sno-dc-broker-development
  namespace: acm-subscriptions
spec:
  clusterSelector:
    matchExpressions:
      - key: purpose
        operator: In
        values:
          - sno
